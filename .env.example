# Copy to .env and fill in. Never commit .env.

NODE_ENV=development
PORT=3000
# Порт на хосте для сервиса bot-trader в docker-compose (проброс на контейнер)
APP_PORT=3000

# Пароль БД в Docker (согласуйте с docker-compose, если меняете)
POSTGRES_USER=trader
POSTGRES_PASSWORD=trader
POSTGRES_DB=trader

# Локально (npm на хосте, БД в Docker):
DATABASE_URL=postgresql://trader:trader@localhost:5432/trader
# Redis: контейнер `make infra` / `docker compose up -d redis` пробрасывает 127.0.0.1:6379 — тогда:
# REDIS_URL=redis://127.0.0.1:6379
# Пусто — без Redis (дедуп Telegram только в одном процессе).
# Контейнер bot-trader: в docker-compose.yml задано REDIS_URL=redis://redis:6379 (перекрывает .env).
REDIS_URL=

# Telegram (from @BotFather)
TELEGRAM_BOT_TOKEN=

# Binance Spot — ключи из API Management (права Spot, без вывода по возможности)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# База Spot API — ОБЯЗАТЕЛЬНО совпадает с типом ключей:
#   продакшен binance.com  → https://api.binance.com (по умолчанию в коде)
#   тестовая сеть          → https://testnet.binance.vision (ключи только с testnet.binance.vision)
# BINANCE_SPOT_BASE_URL=https://api.binance.com
# BINANCE_SPOT_BASE_URL=https://testnet.binance.vision
# Окно timestamp для подписанных запросов (мс), по умолчанию 60000 — меньше ошибок recvWindow при лаге сети/WSL
# BINANCE_RECV_WINDOW_MS=60000
BINANCE_SPOT_SYMBOL=BTCUSDT
# fixed_side — одна сторона (ORDER_SIDE); roundtrip — BUY до TP или SELL учётной позиции (см. README)
BINANCE_SPOT_STRATEGY=fixed_side
# BINANCE_SPOT_STRATEGY=roundtrip
# Порог take-profit к средней цене входа (%, напр. 0.15 = 0.15%)
BINANCE_SPOT_ROUNDTRIP_TAKE_PROFIT_PERCENT=0.15
BINANCE_SPOT_ORDER_SIDE=BUY
# Лимит quote (USDT) на один MARKET BUY при DRY_RUN=false
BINANCE_SPOT_MAX_QUOTE_USDT=20
# Для BINANCE_SPOT_ORDER_SIDE=SELL — объём в базовом активе (напр. BTC)
BINANCE_SPOT_QUANTITY=0

# P2P / исполнение
DRY_RUN=true
EXECUTION_MODE=AUTO_EXCHANGE_ONLY
P2P_PROVIDER=binance

# Стратегия P2P (спред): ASSET/FIAT. RUB с VPS вне РФ у Binance часто даёт пустой стакан — USD/EUR надёжнее.
FIAT=RUB
ASSET=USDT
MIN_SPREAD_PERCENT=0.15
MAX_NOTIONAL_USDT=500
DAILY_MAX_LOSS_USDT=50

# Комиссии (проценты, для расчёта чистого спреда)
P2P_TAKER_FEE_PERCENT=0.0

# TON Connect (Mini App / deep link)
TON_CONNECT_MANIFEST_URL=https://example.com/tonconnect-manifest.json
TON_PAYMENT_RECIPIENT=

# Опционально: ограничить доступ к командам после «оплаты» в TON
REQUIRE_TON_ACCESS=false

# Публичный URL приложения (для ссылок /connect)
PUBLIC_BASE_URL=http://localhost:3000

# Опционально: chat id для алертов по спреду (cron)
TELEGRAM_ALERT_CHAT_ID=

# Список telegram user id админов (через запятую), обход REQUIRE_TON_ACCESS и /autotrade
ADMIN_TELEGRAM_IDS=

# Интервал авто-симуляции (мс), см. /autotrade
AUTO_TRADE_INTERVAL_MS=180000

# Стартовый виртуальный баланс для /stats (USDT)
PAPER_WALLET_START_USDT=10000
